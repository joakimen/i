[package]
name = "i"
version = "0.1.0"
edition = "2024"
description = "Install project dependencies for the toolchains detected in a directory"

[dependencies]
anyhow = "1.0.104"
clap = { version = "4.6.6", features = ["derive"] }
console = "0.16.4"
indicatif = "0.18.6"

[profile.release]
strip = true
